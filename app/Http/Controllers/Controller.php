<?php

namespace App\Http\Controllers;

/**
 * Base Controller — all HWS controllers extend this class.
 * Provides a central place for shared controller logic if needed later.
 * Currently empty per Laravel 11 conventions (no traits required).
 */
abstract class Controller
{
    // Base controller — shared logic can be added here as the system grows
}
